rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Job artifacts: read-only public, write only via Admin SDK
    match /jobs/{jobId}/{allPaths=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
